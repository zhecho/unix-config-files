
# set prefix key to ctrl+a until I have time to adapt
#unbind C-b
#set -g prefix C-a

# send the prefix to client inside window (ala nested sessions)
bind-key a send-prefix

# toggle last window like screen
bind-key C-b last-window

# confirm before killing a window or the server
bind-key k confirm kill-window
bind-key K confirm kill-server

# toggle statusbar
bind-key b set-option status

# ctrl+left/right cycles thru windows
bind-key -n C-right next
bind-key -n C-left prev

# set vi keys
set -g status-keys vi
setw -g mode-keys vi

# open a man page in new window
bind / command-prompt "split-window 'exec man %%'"

# quick view of processes
bind '~' split-window "exec htop"

# scrollback buffer n lines
set -g history-limit 5000

# listen for activity on all windows
set -g bell-action any

# on-screen time for display-panes in ms
set -g display-panes-time 2000

# start window indexing at one instead of zero
set -g base-index 1

# enable wm window titles
set -g set-titles on

# wm window title string (uses statusbar variables)
# keepass get this as assumption 
set -g set-titles-string "tmux.#I.#W"


# Setup sessions 
# new -d -s Session
# neww -n 1 'cd ~/prj' 
# neww -n 2 'cd ~/prj' 
# split-window -h -t 1
# split-window -v -t 1
# resize-pane -D 25
# selectw -t 1
# splitw -v -t 0 
# selectp -t 0 ; send-keys 'cd $HOME/prj/' C-m 
# selectp -t 1 ; send-keys 'cd $HOME/prj/ && tail -F /var/log/system.log &' C-m 
# selectp -t 1 ; send-keys 'cd $HOME/prj/' C-m 
# selectp -t 2 ; send-keys 'cd $HOME/prj/' C-m 

# selectw -t 2; send-keys 'cd $HOME/prj/ && tail -F /var/log/system.log &' C-m 
# selectp -t 2 
# resize-pane -R 5

# attach -t s1

# statusbar --------------------------------------------------------------
set-window-option -g automatic-rename on
set-option -g allow-rename on
setw -g automatic-rename on

bind R source-file ~/.tmux.conf \; display-message "Config reloaded..."

# for version <= 2.5 (currently used 2.3)
# bind -T vi-copy y copy-pipe 'xclip -in -selection clipboard'

# Ctrl+b [ select and hit 'y' and selection will be copied to the clipboard
# bind -T copy-mode-vi y send-keys -X copy-pipe 'xclip -in -selection clipboard'
bind -T copy-mode-vi v send-keys -X begin-selection
# bind -T copy-mode-vi y send-keys -X copy-pipe 'xclip -in -selection clipboard'
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "xclip" 


# scrolling
set -g mouse on
# bind-key -t vi-copy WheelUpPane scroll-up
# bind-key -t vi-copy WheelDownPane scroll-down

# Setup 'v' to begin selection as in Vim
# bind-key -t vi-copy v begin-selection
# bind-key -t vi-copy y copy-pipe "pbcopy"

# Update default binding of `Enter` to also use copy-pipe
# unbind -t vi-copy Enter
# bind-key -t vi-copy Enter copy-pipe "pbcopy"

set -g status-interval 2 
# set -g status-left "#S #[fg=green,bg=black]#(tmux-mem-cpu-load --colors --interval )#[default]"
set -g status-left-length 60

set-option -g status on
set-option -g status-interval 2
set-option -g status-justify centre
set-option -g status-keys vi
set-option -g status-position bottom
set-option -g status-style fg=colour136,bg=default

set-window-option -g window-status-style fg=colour244
set-window-option -g window-status-style bg=default
set-window-option -g window-status-current-style fg=colour166
set-window-option -g window-status-current-style bg=default
# left
set-option -g status-left-length 20
set-option -g status-left-style default
set-option -g status-left "#[fg=green]#H #[fg=black]• #[fg=green,bright] #[default]"
# right 
set-option -g status-right-length 140
set-option -g status-right-style default
set-option -g status-right "#[fg=green,bg=default,bright]#(tmux-mem-cpu-load --interval 3) "
set-option -ag status-right "#[fg=red,dim,bg=default]#(uptime | cut -f 4-5 -d ' ' | cut -f 1 -d ',')
"
set-option -ag status-right " #[fg=blue]%Y-%m-%d #[fg=white,bg=default]%a %l:%M %p#[default] "

# NOTE: in order cpu and mem  to work you need to install plugin with
# NOTE: Install Tmux Plugin manager 
# git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Plugins
# set -g @plugin 'tmux-plugins/tmux-net-speed'

# Plugins config
# set -g @net_speed_interfaces "en0"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
